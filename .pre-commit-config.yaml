repos:
  - repo: local
    hooks:
      - id: detect-secrets
        name: Detect secrets
        entry: ./scripts/pre-commit-secret-check.sh
        language: python
        additional_dependencies:
          - detect-secrets
        pass_filenames: false
        always_run: true

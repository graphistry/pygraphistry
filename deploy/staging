# -*- mode: INI -*-
[all:vars]
# The name of this deploy type (used for Slack notifications)
cluster="staging"
# The MongoDB connection string/URL. Used only by reaper.py; node apps use the `config` node module
# to find their MongoDB URL. The mongodb_dbname is appended to this when connecting.
mongodb_url="mongodb://graphistry:graphtheplanet@lighthouse.2.mongolayer.com:10048,lighthouse.3.mongolayer.com:10048"
# The name of the MongoDB database on compose.io to write pings to
mongodb_dbname="graphistry-staging"

[workers]
staging.graphistry.com

[central]
staging.graphistry.com

[deploy_server]
deploy.graphistry.com

[app_servers:children]
workers
central

[staging:children]
workers
central
